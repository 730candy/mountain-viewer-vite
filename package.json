{
  "name": "mountain-viewer-vite",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "chmod +x node_modules/.bin/vite || echo 'chmod failed, skipping'"
  },
  "devDependencies": {
    "three": "^0.160.1",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  }
}
